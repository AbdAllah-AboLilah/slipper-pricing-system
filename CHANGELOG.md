# v1.3.2 — 2026-08-12

- توحيد رقم الإصدار في جميع الملفات.
- إصلاح استراتيجية Service Worker وتحديث الملفات بدون الاعتماد على Cache قديم.
- إضافة تشخيص تشغيل تلقائي بعد 5 ثوانٍ مع إعادة محاولة وإعادة تهيئة اختيارية.
- منع تشغيل نسخة HTML مع JavaScript من إصدار مختلف عبر cache-busting.
- الحفاظ على النظام Local-First وIndexedDB وModular PWA.

## v1.2.3 — 2026-08-12
- إصلاح توافق IndexedDB مع قواعد البيانات التي أنشأتها v1.2.0 وv1.2.1.
- ترقية آمنة إلى DB version 4 بدون حذف بيانات المستخدم.
- إصلاح وحدة النسخ الاحتياطي.
- تحديث إصدار PWA وCache.

# Changelog

## v1.3.10 — 2026-08-13
- **شاشة البيانات الأساسية** بقت كروت (زي فاتورة التسعير بالظبط): الاسم، النوع، ID، الباركود، المورد، وسعر البيع بعد الخصم ظاهرين مباشرة، وكل الباقي (القسم، سعر البيع قبل الخصم، نسبة/قيمة الخصم، سعر الشراء، التكلفة، LRP، LRB) جوه "تفاصيل الصنف" تفتحها لو حبيت.
- **تصدير ERP بقى xlsx حقيقي** (مش HTML متنكر في xls زي الأول). لو مفيش إنترنت وفشل تحميل محرك الإكسل، بيرجع تلقائيًا لصيغة xls القديمة كبديل مع تنبيه واضح.
- **شاشة التسعير**: الإعدادات فوق بعرض الشاشة كامل، وشرائح الأسعار تحتها بعرض كامل برضه (مش جنب بعض). جدول الشرائح بقى كروت دايمًا على أي مقاس شاشة، فمفيش أي تمرير يمين/شمال خالص.
- تأكيد آلية الربط: كل شريحة سعر بتتربط تلقائيًا بأول صنف ERP له نفس السعر بعد الخصم، ولو أكتر من صنف بنفس السعر بيُطلب منك تختار بنفسك من قائمة عند إدخال الفاتورة (الميزة دي شغالة من v1.3.8).
- **ترتيب أصناف الفاتورة اتعكس**: آخر صنف تضيفه بيظهر أول واحد في الأعلى، مش في الآخر.
- بانتظار صورة خطأ الطباعة الحرارية اللي هترسلها للمراجعة.

## v1.3.9 — 2026-08-13
- **إعادة بناء كاملة لشاشة الفاتورة** بناءً على مراجعتك المباشرة: اتشال الجدول الأفقي الطويل (18 عمود) خالص، وبقى كل صنف "كارت" مستقل زي الموبايل بالظبط، حتى على شاشات الديسك توب.
- الحقول اللي بتدخلها إنت بس ظاهرة في الكارت (النوع، قطع الدستة، عدد الدستات، سعر شراء الدستة، خصم المورد %، سعر بيع يدوي اختياري).
- شريط نتيجة واحد وواضح: اسم صنف ERP + سعر البيع + الإجمالي بخط كبير — من غير أي أرقام حساب وسطية تشوش.
- كل الأرقام الوسطية (سعر الشراء/قطعة، التكلفة/قطعة، أساس التسعير، السعر قبل التقريب) + الباركود + الـID + الملاحظات بقوا جوه "تفاصيل الحساب" تفتحها بضغطة لو حبيت تتأكد.
- حالة السطر (مطابق / أكثر من صنف / لا توجد شريحة...) بقت لون حدود الكارت نفسه (أخضر/برتقالي/أحمر) بدل عمود منفصل.

## v1.3.8 — 2026-08-13
- **قالب تصدير ERP اتصحح للـ7 أعمدة الحقيقية** من شاشة "إضافة البيانات للسندات": ID، الباركود، اسم الصنف، الوحدة (قطعة - ثابتة)، السعر (= التكلفة/قطعة)، الكمية، سعر البيع (= السعر بعد الخصم). ممكن ترجع للقالب القديم أو تعدّل أي عمود من شاشة "ترتيب أعمدة ERP".
- **اكتشاف مهم من ملف أصناف المخزن الحقيقي**: أصناف "Winter/foot wear" مُسمّاة بسعرها فتُطابَق تلقائيًا وبثقة، لكن أصناف "سليبر" أسماء منتجات حقيقية (زي luna 444 وluna 454) وأكتر من صنف بيشتركوا في نفس السعر بعد الخصم. النظام كان يختار أول صنف مطابق بصمت بدون تنبيه — دلوقتي لما يكون فيه أكتر من صنف بنفس السعر، خانة "اسم ERP" في سطر الفاتورة بتتحول لقائمة اختيار حقيقية تقدر تحدد منها الصنف الصحيح.
- تحسين خوارزمية تخمين الأعمدة عند استيراد ملفات إكسل حقيقية: كانت بتتعارض بين "القسم" و"القسم الرئيسي"، وبين "السعر" و"السعر بعد الخصم"، خاصة مع مسافات غير منتظمة في العناوين. أضيفت أيضًا حقول "نسبة الخصم %" و"قيمة الخصم" كحقول قابلة للاستيراد والحفظ.

## v1.3.7 — 2026-08-13
- إصلاح خطأ في صورة الفاتورة المُشارَكة كان يطبع النص `v${APP_VERSION}` حرفيًا بدل رقم الإصدار الفعلي (غلطة أقواس تنصيص).
- إضافة اختيار حقيقي لمقاس الطباعة الحرارية (58مم أو 80مم) بجانب زر الطباعة، بعد ما كان النظام يدّعي دعم الاثنين والكود فعليًا بيطبع 80مم بس.

## v1.3.6 — 2026-08-13
- **إعادة ملاءمة الشاشات**: نقطة التحول بين عرض الجدول الأفقي وعرض الكروت رُفعت من 760px إلى 1180px، فمعظم شاشات اللاب توب هتاخد عرض كروت بدون أي تمرير جانبي بدل الجدول الواسع.
- تقليل حشو الأعمدة وحجم الخط في كل الجداول، وتثبيت أول عمود (الرقم) أثناء التمرير الأفقي في الجداول اللي لسه محتاجة تمرير (على الشاشات العريضة فقط).
- شاشة شرائح الأسعار بقى لها عرض كروت مخصص على الموبايل والشاشات المتوسطة، بدل جدول واحد بس.
- **إضافة قواعد التسعير من معادلة الإكسل**: زرار "تحميل شرائح Slipper الافتراضية" و"تحميل شرائح Winter الافتراضية" في شاشة التسعير، بيحمّل نفس شرائح ملف الإكسل الأصلي (VLOOKUP/IF) بعد تصحيح كل التداخلات والفجوات فيها يدويًا. التحميل اختياري وقابل للتعديل/الحذف بالكامل بعدها، ومفيش أي زرع تلقائي إجباري.
- إصلاح مرجع قديم لرقم إصدار Service Worker كان مكتوب ثابت (1.3.2) بدل ما يتبع رقم الإصدار الحالي تلقائيًا.

## v1.3.5 — 2026-08-13
- **السبب الجذري لمشكلة "جاري تشغيل النظام" اتلقى ✅**: كان فيه قوس زيادة `)` في دالة `readWorkbook` بملف `src/app.js` (سطر استيراد Excel)، وده كان يعتبر خطأ Syntax حقيقي يمنع تحميل الملف كاملًا كوحدة JavaScript Module — فيفضل النظام واقف على الشاشة الأولى للأبد بدون أي تفاعل، لأن الملف كله (مش سطر واحد بس) كان يرفض التحميل.
- تم التأكد بعد الإصلاح إن كل الأقواس في الملف متطابقة تمامًا (932 فتح = 932 قفل)، وفحص Syntax لكل ملفات JavaScript اجتاز بنجاح.
- شريط الأخطاء الأحمر اللي أُضيف في v1.3.4 هو اللي كشف المكان بالظبط (app.js:140)، وهيفضل موجود يفيدنا في أي مشكلة مستقبلية.

## v1.3.4 — 2026-08-13
- أُضيف شريط أخطاء ظاهر أسفل الشاشة (لون أحمر) يعرض تلقائيًا أي خطأ JavaScript أو ملف فشل تحميله (404) أو أي Promise فشلت، مع زر "إخفاء". هذا الشريط مكتوب بكود مستقل تمامًا عن باقي النظام، فيظهر حتى لو فشل تحميل app.js نفسه بالكامل.
- إضافة تقارير أخطاء يدوية أوضح عند فشل تهيئة قاعدة البيانات المحلية أثناء الإقلاع.

## v1.3.3 — 2026-08-13
- أُضيف سكريبت تلقائي (يعمل قبل أي حاجة تانية، وبدون الاعتماد على وحدات الـJS) يمسح أي Service Worker وأي كاش قديم من نسخ سابقة (v1.2.x وما قبلها) عند أول زيارة، ثم يعمل تحديث تلقائي مرة واحدة فقط.
- هدف هذا الإصدار حصريًا: حل مشكلة "جاري تشغيل النظام" التي لا تختفي رغم رفع نسخ أحدث، والناتجة عن Service Worker قديم كان يعترض تحميل الصفحة الجديدة ويعرض نسخة قديمة من الكاش.
- لم يتم تعديل أي منطق تسعير أو شاشات — هذا إصدار إصلاح نشر (deployment fix) فقط.


## v1.2.2 — 2026-08-12
- إصلاح مشكلة شاشة البدء البيضاء بعد الانتقال من v1.1.0.
- تثبيت إصدار IndexedDB على نفس مخطط v1.1.0 لتجنب ترقية غير ضرورية قد تعلق عند وجود نسخة مفتوحة.
- جعل بدء التشغيل ينتظر عملية الرسم الفعلية ويعرض رسالة خطأ مفهومة بدل ترك الشاشة فارغة.
- تحديث Service Worker/cache إلى v1.2.2.
- إصلاح اعتماد وحدة النسخ الاحتياطي على قائمة STORES.

# سجل الإصدارات

## v1.2.0 — 2026-08-12
- النظام يبدأ بدون أي موردين أو أصناف أو شرائح أسعار تجريبية.
- ترحيل النسخة القديمة يمسح بيانات seed فقط ويحافظ على أي بيانات حقيقية.
- إضافة فصل صريح بين سعر الشراء والتكلفة الفعلية.
- خصم المورد يحسب التكلفة تلقائيًا؛ مثال 50 مع خصم 5% = 47.50.
- إعداد قابل للتحديد: حساب نسبة الربح على التكلفة أو سعر الشراء.
- تثبيت تعريف سعر البيع = السعر قبل الخصم كقيمة واحدة، مع سعر بعد الخصم مستقل.
- إضافة حقول بيانات ERP: ID، Barcode، المورد، القسم الرئيسي، القسم الفرعي، السعر، البيع/قبل الخصم، بعد الخصم، الشراء، التكلفة، LRP، LRB.
- استيراد ERP جديد بخطوات: اختيار الملف، الورقة، صف عناوين الأعمدة، Mapping، معاينة، ثم تأكيد.
- حفظ Mapping وإمكانية تحديث الأصناف حسب ID.
- استخراج الموردين تلقائيًا من ملف البيانات ومنع تكرارهم.
- واجهة بيانات أساسية Responsive بدون زحلقة على الهاتف؛ الجدول يتحول إلى بطاقات.
- واجهة الفاتورة على الهاتف تتحول إلى بطاقات لكل سطر بدل الجدول العريض.
- تحسين الحفظ التلقائي أثناء الكتابة إلى IndexedDB.
- إضافة LRP/LRB إلى قاعدة البيانات وإمكانية إدراجهما في تصدير ERP.
- إصدار النظام أصبح v1.2.0.

## v1.1.0 — 2026-08-12
- تصحيح اسم المنشئ إلى `عبد الله <Abo Lilah>`.
- تحويل النظام إلى Modular PWA أوضح مع وحدات مستقلة.
- IndexedDB v2 مع الحفاظ على البيانات الموجودة عند التحديث.
- حفظ تلقائي سريع جدًا أثناء الكتابة واستعادة آخر مسودة.
- رقم فاتورة يومي تلقائي وقابل لتغيير الـPrefix.
- تاريخ المسودة دائمًا تاريخ اليوم، ويُثبت عند الحفظ النهائي.
- حالة ERP ورقم فاتورة ERP مع تحقق قبل الحفظ.
- نوع التسعير الافتراضي Slipper/Winter محفوظ حتى تغييره.
- كل سطر يحتفظ بنوعه، والسطر الجديد يرث النوع الافتراضي.
- فصل نسبة الربح عن شرائح الأسعار.
- كشف الفجوات والتداخلات وتعدد نتائج البحث بدل إخفائها.
- بحث داخلي بديل عن VLOOKUP مع نفس فكرة السعر النهائي → صنف ERP.
- إدارة الموردين والأصناف + استيراد CSV/Excel عند توفر SheetJS.
- ترتيب أعمدة تصدير ERP قابل للتخصيص والحفظ محليًا.
- تصدير ERP وCSV.
- صورة ومشاركة الفاتورة عبر Share API عند توفرها.
- مرفق أصل الفاتورة صورة/PDF محفوظ مع الفاتورة.
- طباعة حرارية 58/80mm مع منطقة أمان داخلية.
- دعم الشاشات الصغيرة وiPhone/Android والكمبيوتر.
- نسخ احتياطي واستعادة تشمل المرفقات.
- Service Worker cache مرتبط برقم الإصدار لتسهيل تحديث GitHub Pages.
- إضافة معلومات الإصدار والـBuild والمنشئ في الواجهة.

## v1.0.0
- أول نسخة من النظام.