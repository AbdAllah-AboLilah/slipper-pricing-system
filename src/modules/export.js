import {excelHtmlBlob,xlsxBlob,downloadBlob,csvEscape,toast} from '../core/utils.js';
const valueFor=(invoice,l,i,key)=>({seq:i+1,invoiceNo:invoice.invoiceNo,date:invoice.invoiceDate,supplier:invoice.supplierName||'',supplierId:invoice.supplierId||'',type:l.type||invoice.type,itemName:l.erpName||'',itemId:l.erpItemId||l.itemId||'',barcode:l.barcode||'',lrp:l.lrp||'',lrb:l.lrb||'',qty:l.pieces||0,unit:l.unit||0,unitLabel:'قطعة',cartonQty:l.qty||0,purchasePrice:l.purchasePerPiece||0,cost:l.costPerPiece||0,purchaseCartonPrice:l.purchaseCartonPrice||0,sellPrice:l.finalPrice||0,saleBeforeDiscount:l.itemSaleBeforeDiscount??'',saleAfterDiscount:l.finalPrice||0,total:l.total||0,profit:l.targetProfit||0,margin:l.targetMargin||0,notes:l.notes||''}[key]??l[key]??'');
export async function exportERP(invoice,columns){const headers=columns.map(c=>c.label);const rows=invoice.lines.map((l,i)=>columns.map(c=>valueFor(invoice,l,i,c.key)));try{downloadBlob(await xlsxBlob(headers,rows,'ERP'),`${invoice.invoiceNo}.xlsx`)}catch(e){console.error(e);toast('تعذر إنشاء xlsx، تم التصدير بصيغة xls بديلة (بلا إنترنت)','error');downloadBlob(excelHtmlBlob(headers,rows,'ERP Invoice Export'),`${invoice.invoiceNo}.xls`)}}
export function exportCSV(invoice,columns){const rows=invoice.lines.map((l,i)=>columns.map(c=>valueFor(invoice,l,i,c.key)));const txt='\uFEFF'+[columns.map(c=>csvEscape(c.label)).join(','),...rows.map(r=>r.map(csvEscape).join(','))].join('\n');downloadBlob(new Blob([txt],{type:'text/csv;charset=utf-8'}),`${invoice.invoiceNo}.csv`)}
